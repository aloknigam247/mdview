# Code Samples

A rust sample:

```rust
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}
```

A python sample:

```python
def greet(name: str) -> str:
    return f"hello {name}"
```

Inline `let x = 1;` form.
