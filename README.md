# mdview

A lightweight, extensible markdown renderer in Rust with a Tauri desktop app (default), a terminal pager (`--terminal`), and a Neovim live-preview plugin. Full GFM plus built-in mermaid, draw.io, plotly, math, and syntax-highlighted code. Modern, sleek, curved. See `CLAUDE.md` for architecture; full documentation arrives with Unit 17.
